# Data Directory

This directory contains all data files for Stratford AI.

## Structure

- `csv/` - Historical market data in CSV format
- `cache/` - Cached API responses and processed data
- `exports/` - Exported backtest results and reports
- `backtests/` - Saved backtest configurations and results
- `logs/` - Application logs and audit trails

## Important Notes

- All data files are ignored by git except this .gitkeep file
- In production, ensure proper backup and retention policies
- Sensitive data should be encrypted at rest